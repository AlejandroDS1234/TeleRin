#Archivo principal de componentes
en esta carpeta se hallan algunos de los principales componentes utilizados para maquetar la pagina,en caso tal de necesitar cambiar un componente y su funcionamiento esta es la carpeta optima para ello, en caso tal que lo que necesite sea cambiar su vision,dispocision,pocision,tamaño,si se desnuda o no,etc lo mejor es acomodarlo directamente en la pagina en la que se este matando (a menos que quieran que un componente o algo encerio se desnude si el usuario paga, ahi si toca modificar el comportamiento del componente y animarlo, y eso lo hara el que lo propuso, no voy a animar nudes de un componente y al que me diga rive le meto un bate por el pene, primer aviso)
